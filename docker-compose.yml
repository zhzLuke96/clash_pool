services:
  clash-pool:
    build: .
    command: [""]
    network_mode: host
    # ports:
    #   - "3000:3000"
    #   - "52000-53000:52000-53000"
    volumes:
      - ./data:/app/data
    environment:
      - TEST_URL=http://www.cloudflare.com/cdn-cgi/trace # 可自定义测速地址
    restart: unless-stopped
